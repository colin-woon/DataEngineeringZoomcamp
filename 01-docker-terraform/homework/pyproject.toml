[project]
name = "homework"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Colin Woon", email = "colin.wyj@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.5",
    "psycopg>=3.3.4",
    "pyarrow>=25.0.1",
    "sqlalchemy>=2.0.52",
]

[project.scripts]
homework = "homework:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
